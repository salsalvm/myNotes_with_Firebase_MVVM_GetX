import 'package:flutter/material.dart';

//this class is hold whole color
//its a static variable we can call anywhere with help of class name
class KColors {
  static const Color kPrimary = Color.fromRGBO(48, 164, 248, 1);
  static const Color kSecondary = Color.fromRGBO(7, 97, 165, 0.9);

  static const Color kError = Color.fromARGB(255, 255, 0, 0);
  static const Color kSuccess = Color.fromARGB(217, 0, 220, 29);
  static const Color kWarnning = Color(0xffFBB040);
  static const Color kMuted = Color.fromARGB(255, 208, 205, 205);

  static const Color kTransperent = Colors.transparent;
  static const Color kWhite = Colors.white;
  static const Color kBlack = Colors.black;
  static const Color kGrey = Colors.grey;
}
